package src.randomTask;

import java.util.List;
import java.util.Optional;
import java.util.stream.Collectors;

public class Main {
    public static void main(String[] args) {
/*        List<Integer> integers = List.of(1, 3, 6, 8, 10, 18, 36);
        Integer i = integers.stream()
                .reduce(Integer::sum).orElse(0);
        System.out.println(i/integers.size());*/


/*        List<String> strings = List.of("RED", "grEEn", "white", "Orange", "pink");
        List<Object> collectLowerCase = strings.stream()
                .map(String::toLowerCase)
                .collect(Collectors.toList());
        List<Object> collectUpperCase = strings.stream()
                .map(String::toUpperCase)
                .collect(Collectors.toList());
        System.out.println(collectLowerCase);
        System.out.println(collectUpperCase);*/


    }
}
